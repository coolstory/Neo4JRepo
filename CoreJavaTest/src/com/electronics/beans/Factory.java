/* CreateOrders (set values of all the attributes) and put them in an order queue 
which will be processed according to FIFO strategy, this method should be synchronized
and set default order status as “Placed”. Use a deviceManufature(String deviceType) 
to manufacture any device object.*/

package com.electronics.beans;

import java.util.ArrayList;
import java.util.List;

public class Factory {
	ElectronicsFactory factory;
	Customer customer;
	
	public void createOrders(int customerId, int orderId, String deviceType){
		factory.deviceManufacturer("laptop");
		if(customer.getCustomerId() == 1){
			
		
	
	
		
		
		}
	}

}
